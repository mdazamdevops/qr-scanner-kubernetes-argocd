apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: qr-scanner-app
  labels:
    app: frontend
    component: nodejs
spec:
  selector:
    app: frontend  # ← Make sure this matches pod labels!
    component: nodejs
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP