apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: qr-scanner-app
  labels:
    app: backend
    component: python-flask
spec:
  selector:
    app: backend  # ← This is the critical fix!
    component: python-flask
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP